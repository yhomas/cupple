
# Flutter + Firebase 開発技術設計書 (Technical Design Document)
本ドキュメントは、夫婦向け感謝共有アプリの実装において、OpenHands（AIエージェント）および開発チームが共通して従うべき技術設計・アーキテクチャ・コーディング規約を定義します。
## 1. 全体アーキテクチャ (Architecture Overview)
メンテナンス性とテスト容易性を高めるため、機能単位でコードを分割する **Feature-First (機能ファースト) スキーム** を採用します。
### 1.1 層（Layer）の分割
各機能（Feature）フォルダ配下は、簡易クリーンアーキテクチャに基づき、以下の3層に分割します。
 1. **Presentation Layer (UI/表現層)**:
   * Widget、画面（Screen）、およびRiverpodの Notifier (Controller)
   * ユーザーインタラクションの検知と状態の描画のみに専念する
 2. **Domain Layer (ドメイン/ビジネスロジック層)**:
   * データモデル（Entity/Class）
   * ビジネスルールの定義（検証ロジックなど）
 3. **Data Layer (データ/インフラ層)**:
   * Firebase（Firestore/Auth）との通信を行う Repository クラス
   * 外部APIクライアントやローカルストレージへのアクセス
## 2. ディレクトリ構造 (Directory Structure)
プロジェクトルート配下の lib フォルダは以下の構造でセットアップします。OpenHandsは新しい機能を実装する際、この構造を厳守してください。
```text
lib/
├── main.dart                  # アプリのエントリーポイント（Firebase初期化など）
├── app.dart                   # MaterialAppクラス、テーマ設定、ルート分岐
├── core/                      # アプリ全体で共有するコアモジュール
│   ├── theme/                 # カラーパレット、Material 3デザイン定義
│   ├── router/                # 画面遷移管理（GoRouter またはシンプルなNavigator）
│   └── widgets/               # レスポンシブコンテナ、共通ボタン等
└── features/                  # 機能単位のディレクトリ
    ├── auth/                  # 1. 認証機能 (ログイン/サインアップ)
    │   ├── data/              # auth_repository.dart
    │   ├── domain/            # user_model.dart
    │   └── presentation/      # login_screen.dart, auth_controller.dart
    ├── couple/                # 2. パートナー連携機能 (招待コード生成・紐付け)
    │   ├── data/              # couple_repository.dart
    │   ├── domain/            # couple_model.dart
    │   └── presentation/      # link_partner_screen.dart, couple_controller.dart
    ├── cards/                 # 3. 感謝＆やったよカード (投稿・タイムライン・リアクション)
    │   ├── data/              # cards_repository.dart
    │   ├── domain/            # card_model.dart
    │   └── presentation/      # timeline_screen.dart, post_card_screen.dart, card_controller.dart
    └── report/                # 4. 分析レポート機能 (集計・統計)
        ├── data/
        ├── domain/
        └── presentation/      # report_screen.dart, report_controller.dart
```
## 3. 状態管理設計 (Riverpod State Management)
Riverpod 2.x（Generatorを推奨）を使用し、状態を管理します。
### 3.1 推奨アノテーション形式
コード自動生成ツール（build_runner）を活用するため、@riverpod アノテーションを使用します。
### 3.2 状態フローの規約
 * **UI (Widget)** は ref.watch(provider) で状態を監視し、AsyncValue を利用して Loading/Error/Data 状態をハンドリングします。
 * ユーザーのアクション（例: 投稿ボタンタップ）は、ref.read(provider.notifier).method() を経由してデータ層（Repository）へ通知します。
 * **例外処理**: Controller/Notifier 内で try-catch し、UI層に安全にエラーを伝える仕組みを構築します。
## 4. Firebase 連携 ＆ セキュリティルール (Firebase Setup)
### 4.1 Firestore データ設計
 * users コレクション: ドキュメントID = Firebase Authの uid
 * couples コレクション: ドキュメントID = 自動生成（UUID）
 * cards コレクション: ドキュメントID = 自動生成（UUID）
### 4.2 セキュリティルール (Firestore Security Rules)
夫婦間のプライベートデータを守るため、セキュアなルールを策定します。
## 5. レスポンシブUI設計 (Mobile & Web Compatibility)
 * MaxWidthContainer (最大幅600px) によるレスポンシブ対応
 * モバイル: NavigationBar, Web/デスクトップ: NavigationRail
## 6. 実装プロセス（OpenHandsへの指示チェックリスト）
 * Step 1: プロジェクト作成 & Firebase初期化
 * Step 2: 基礎デザインテーマ (Theme) 定義
 * Step 3: 認証・ユーザー登録・連携機能の構築
 * Step 4: タイムラインと投稿画面の実装
 * Step 5: クイック承認リアクション機能の実装
 * Step 6: レスポンシブ最適化 ＆ Webデプロイ検証
